* Folder is placeholder for "(dynamic) page content." 
* Currently, (static) content is @ app/common/constants.js.

NOTE: Dynamic or Static "Content" is rendered in a "Page." 
      Page is used for web context. Screen is used for mobile context.